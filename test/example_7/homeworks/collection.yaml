schema:
    required_artifacts:
        - homework.pdf
        - solution.pdf

    optional_artifacts:
        - template.zip

    metadata_schema:
        required_keys:
          name:
              value_schema:
                  type: string
          due:
              value_schema:
                  type: date
          released:
              value_schema:
                  type: date

    is_ordered: true
