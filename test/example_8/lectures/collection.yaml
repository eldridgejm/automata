schema:
    required_artifacts: []

    optional_artifacts:
        - template.zip

    metadata_schema:
        required_keys:
          name:
              value_schema:
                  type: string
          date:
              value_schema:
                  type: datetime

    is_ordered: true
