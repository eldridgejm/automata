schema:
    required_artifacts: []

    optional_artifacts:
        - template.zip

    metadata_schema:
        name:
            type: string
        date: 
            type: datetime

    is_ordered: true
