# Example Course

This directory contains a sample course designed to demonstrate the features and capabilities of `automata`. It includes example `collection.yaml` and `publication.yaml` files, as well as a sample website configuration.
