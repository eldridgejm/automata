publication_schema:
    required_artifacts:
        - lab.txt
        - solution.txt

    metadata_schema:
        required_keys:
            due:
                type: datetime
            released:
                type: datetime
            number:
                type: integer
